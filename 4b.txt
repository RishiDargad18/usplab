gcc -o echoall echoall.c chmod 777 textinterpreter gcc -o inter inter.c ./inter

echoall.c
#include <stdio.h>
int main(int argc, char *argv[]){
    for (int i = 0; i < argc; i++)
        printf("argv[%d]= %s\n", i, argv[i]);
    return 0;
}

inter.c
#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>

int main(){
    if (fork() == 0){
        execl("./textinterpreter", "textinterpreter",
              "myarg1", "myarg2", "myarg4", (char *)0);
        printf("error\n");
    }
    wait(NULL);
    return 0;
}

textinterpreter
#!/home/your_username/echoall my2
